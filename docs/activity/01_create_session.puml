@startuml 01_create_session
title Создание новой сессии интервью

start

:Пользователь передаёт scriptId;

:Загрузить скрипт из БД;

if (Скрипт найден и валиден?) then (нет)
  :Ошибка NOT_FOUND / BAD_REQUEST;
  stop
endif

if (Активная сессия уже существует?) then (да)
  :Вернуть ID существующей сессии;
  stop
endif

:Создать сессию и первый вопрос в БД;

:Вернуть sessionId;

stop
@enduml
